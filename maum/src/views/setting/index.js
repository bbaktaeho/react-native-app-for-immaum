import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Setting = props => {
  return (
    <SafeAreaView>
      <Text>설정</Text>
    </SafeAreaView>
  );
};

export default Setting;
